{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://alerts-bi.internal/schemas/teams.schema.json",
  "title": "Alerts BI team registry",
  "description": "Versioned ownership registry (design section 3.1). Ownership is a supplied list of operator values; nothing is parsed or inferred to establish it.",
  "type": "object",
  "additionalProperties": false,
  "required": ["registry_version", "teams"],
  "properties": {
    "registry_version": {
      "type": "string",
      "minLength": 1,
      "description": "Bumped whenever any ownership mapping changes, so 'their behaviour changed' stays distinguishable from 'we edited the mapping'."
    },
    "effective_date": {
      "type": "string",
      "format": "date",
      "description": "Date this registry version took effect (UTC calendar date)."
    },
    "teams": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/team" }
    }
  },
  "$defs": {
    "team": {
      "type": "object",
      "additionalProperties": false,
      "required": ["team_id", "display_name", "v1_operators", "v2_operator"],
      "properties": {
        "team_id": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9-]*$",
          "description": "Stable identifier. Never reused for a different team."
        },
        "display_name": { "type": "string", "minLength": 1 },
        "v1_operators": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 },
          "description": "Exact, case-sensitive v1 operator values. May be empty after migration. Case variants used by one team are listed explicitly."
        },
        "v2_operator": {
          "type": ["string", "null"],
          "minLength": 1,
          "description": "Exact v2 operator, derived from the team's API key. Null before migration."
        },
        "panels": {
          "type": "array",
          "items": { "$ref": "#/$defs/panel" },
          "description": "Optional supplied Grafana panel queries, used only for suppression detection (design section 5.2). Never for ownership."
        }
      }
    },
    "panel": {
      "type": "object",
      "additionalProperties": false,
      "required": ["panel_id", "schema", "sql"],
      "properties": {
        "panel_id": { "type": "string", "minLength": 1 },
        "schema": { "enum": ["v1", "v2"] },
        "sql": { "type": "string", "minLength": 1 },
        "variables": {
          "type": "array",
          "items": { "$ref": "#/$defs/variable" },
          "description": "Frozen variable definitions supplied with the registry. The MVP never calls Grafana."
        }
      }
    },
    "variable": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "type"],
      "properties": {
        "name": { "type": "string", "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" },
        "type": {
          "enum": ["custom", "constant", "interval", "query"],
          "description": "Only custom, constant and interval resolve. A query variable is never executed and makes its suppression leaf unmeasured."
        },
        "value": {
          "type": "string",
          "description": "Single resolved value for constant/interval variables."
        },
        "values": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Complete selected-value list for a multi-value custom variable."
        },
        "multi": { "type": "boolean" },
        "all_selected": {
          "type": "boolean",
          "description": "True when the panel had '$__all' selected. Expanding to everything is exactly the blast-radius failure mode guarded in design section 5.2."
        }
      }
    }
  }
}
