node_modules
coverage
out
package-lock.json

# Markdown is authored for reading, not for a formatter to rewrap.
*.md

# Generated output of the mock seeder.
mock-data-stats.json

# The Compose file contains a shell healthcheck whose quoting Prettier rewrites in a way
# the YAML parser then rejects. It is validated by `docker compose config` instead.
docker-compose.yml

# Fixture tooling. Alert definitions are deliberately one dense line each so a reviewer can
# scan the dataset as a table; Prettier would explode every one across eight lines and make
# the fixtures far harder to read and diff. These files are linted and type-checked.
scripts/
